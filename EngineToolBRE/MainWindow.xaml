<Window x:Class="EngineToolBRE.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="BRE Unity Template Tool"
        Height="600" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource App.Background}">

    
    <Window.Resources>
        
        <SolidColorBrush x:Key="App.Accent"      Color="#5B8DEF"/>
        <SolidColorBrush x:Key="App.Accent.Dim"  Color="#4A79D9"/>
        <SolidColorBrush x:Key="App.Background"  Color="#0F1115"/>
        <SolidColorBrush x:Key="App.Surface"     Color="#171A21"/>
        <SolidColorBrush x:Key="App.Card"        Color="#1E2430"/>
        <SolidColorBrush x:Key="App.Border"      Color="#273043"/>
        <SolidColorBrush x:Key="App.Text"        Color="#E6EAF2"/>
        <SolidColorBrush x:Key="App.Text.Muted"  Color="#B6BECF"/>
        <SolidColorBrush x:Key="App.Success"     Color="#20C997"/>
        <SolidColorBrush x:Key="App.Warn"        Color="#FFB020"/>

        
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <!-- Card-Container -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource App.Card}"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="0" Opacity="0.25" Color="#000000"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Label oberhalb von Eingaben -->
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource App.Text.Muted}"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
            <Setter Property="Background" Value="{StaticResource App.Surface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
            <Setter Property="Background" Value="{StaticResource App.Surface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid SnapsToDevicePixels="True">
                            
                            <Border x:Name="Outer"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="8"/>

                            
                            <Grid Margin="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition Width="32"/>
                                </Grid.ColumnDefinitions>

                                <!-- Anzeige des ausgewählten Eintrags -->
                                <ContentPresenter x:Name="ContentSite"
                              Margin="10,0,4,0"
                              VerticalAlignment="Center"
                              Content="{TemplateBinding SelectionBoxItem}"
                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                              IsHitTestVisible="False"/>

                                <!-- DropDown Button -->
                                <ToggleButton x:Name="DropDownToggle"
                          Grid.Column="1"
                          BorderThickness="0"
                          Background="Transparent"
                          Focusable="False"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                    <TextBlock FontFamily="Segoe MDL2 Assets"
                         Text="&#xE70D;"
                         Foreground="{StaticResource App.Text.Muted}"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"/>
                                </ToggleButton>
                            </Grid>

                            <!-- Popup -->
                            <Popup x:Name="PART_Popup"
                 Placement="Bottom"
                 IsOpen="{TemplateBinding IsDropDownOpen}"
                 AllowsTransparency="True"
                 Focusable="False"
                 PopupAnimation="Slide">
                                <Border Background="{StaticResource App.Surface}"
                    BorderBrush="{StaticResource App.Border}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="4"
                    SnapsToDevicePixels="True">
                                    <ScrollViewer MaxHeight="260">
                                        
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <!-- Triggers -->
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="ContentSite" Property="TextElement.Foreground" Value="{StaticResource App.Text.Muted}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Outer" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Outer" Property="BorderBrush" Value="{StaticResource App.Accent}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

            <!-- Items im Dropdown -->
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
                        <Setter Property="Padding" Value="10,6"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ComboBoxItem">
                                    <Border x:Name="ItemBorder" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#22334D"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="Background" Value="#2A3C5C"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Opacity" Value="0.6"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>


        <!-- Primärer Button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource App.Accent}"/>
            <Setter Property="BorderBrush" Value="{StaticResource App.Accent.Dim}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MinHeight" Value="38"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" CornerRadius="9"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource App.Accent.Dim}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Sekundärer Button -->
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Foreground" Value="{StaticResource App.Text}"/>
            <Setter Property="Background" Value="{StaticResource App.Surface}"/>
            <Setter Property="BorderBrush" Value="{StaticResource App.Border}"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <!-- Mini-Icon-Button -->
        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="MinWidth" Value="36"/>
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                   Text="{Binding}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{StaticResource App.Text}"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="WatermarkedTextBox" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                CornerRadius="8">
                            <Grid>
                                
                                <ScrollViewer x:Name="PART_ContentHost"/>

                                
                                <TextBlock x:Name="PART_Watermark"
                       Text="{TemplateBinding Tag}"
                       Foreground="{StaticResource App.Text.Muted}"
                       Margin="12,0,0,0"
                       VerticalAlignment="Center"
                       IsHitTestVisible="False"
                       Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsKeyboardFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="PART_Watermark" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <!-- ================= Layout ================= -->
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="2,0,2,16" VerticalAlignment="Center">
            <Border Background="{StaticResource App.Accent}" CornerRadius="8" Width="36" Height="36" Margin="0,0,10,0">
                <TextBlock Text="&#xE7C3;" FontFamily="Segoe MDL2 Assets"
                   Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <StackPanel>
                <TextBlock Text="BRE Unity Template Tool" FontSize="18" FontWeight="Bold"/>
                <TextBlock Text="Erstelle saubere Unity-Projekte mit einem Klick"
                   Foreground="{StaticResource App.Text.Muted}" FontSize="12"/>
            </StackPanel>
        </StackPanel>

        <!-- Formular -->
        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,12,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Unity Version -->
                <TextBlock Grid.Row="0" Text="Unity-Version" Style="{StaticResource FormLabel}"/>
                <DockPanel Grid.Row="0" Grid.ColumnSpan="2">
                    <ComboBox ItemsSource="{Binding UnityInstallations}"
                    SelectedItem="{Binding SelectedUnity}"
                    DisplayMemberPath="DisplayString"
                    Margin="0,0,8,0" />
                    <Button Style="{StaticResource IconButton}" Content="&#xE72C;"
                  ToolTip="Neu scannen"
                  Command="{Binding RescanCommand}"/>
                </DockPanel>

                <!-- Template -->
                <TextBlock Grid.Row="1" Text="Template" Style="{StaticResource FormLabel}" Margin="0,12,0,0"/>
                <ComboBox Grid.Row="1" Grid.ColumnSpan="2"
                  ItemsSource="{Binding Templates}"
                  SelectedItem="{Binding SelectedTemplate}"
                  DisplayMemberPath="Name"/>

                <!-- Template Description -->
                <Border Grid.Row="2" Grid.ColumnSpan="2" Background="{StaticResource App.Surface}"
                BorderBrush="{StaticResource App.Border}" BorderThickness="1"
                CornerRadius="8" Padding="10" Margin="0,8,0,0">
                    <TextBlock Text="{Binding SelectedTemplate.Description}"
                     Foreground="{StaticResource App.Text.Muted}" />
                </Border>

                <!-- Projektname -->
                <TextBlock Grid.Row="3" Text="Projektname" Style="{StaticResource FormLabel}" Margin="0,12,0,0"/>
                <TextBox Grid.Row="3" Grid.ColumnSpan="2" Tag="z. B. MyGame_Proto"
                 Style="{StaticResource WatermarkedTextBox}"
                 Text="{Binding ProjectName}" />

                <!-- Zielordner -->
                <TextBlock Grid.Row="4" Text="Zielordner" Style="{StaticResource FormLabel}" Margin="0,12,0,0"/>
                <Grid Grid.Row="4" Grid.ColumnSpan="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" IsReadOnly="True" Text="{Binding TargetDirectory}"/>
                    <Button Grid.Column="1" Margin="8,0,0,0" Style="{StaticResource IconButton}"
                  Content="&#xE8B7;" ToolTip="Ordner wählen…"
                  Command="{Binding BrowseFolderCommand}"/>
                </Grid>

                <!-- Spacer -->
                <Grid Grid.Row="5"/>
            </Grid>
        </Border>

        <!-- Ausgabe / Log -->
        <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Card}" Margin="12,0,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                    <TextBlock Text="Ausgabe" FontWeight="SemiBold" FontSize="14"/>
                    <TextBlock Text="  •  " Foreground="{StaticResource App.Text.Muted}" Margin="4,0"/>
                    <TextBlock Text="{Binding SelectedUnity.DisplayString}"
                     Foreground="{StaticResource App.Text.Muted}" />
                </StackPanel>

                <Border Grid.Row="1" Background="{StaticResource App.Surface}"
                BorderBrush="{StaticResource App.Border}" BorderThickness="1"
                CornerRadius="8">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <TextBox Text="{Binding LogText}" TextWrapping="Wrap"
                     Background="{StaticResource App.Surface}"
                     BorderThickness="0"
                     IsReadOnly="True" AcceptsReturn="True"
                     Foreground="{StaticResource App.Text}" />
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- Footer / Actions -->
        <DockPanel Grid.Row="2" Grid.ColumnSpan="2" Margin="0,16,0,0">
            <TextBlock Text="Tipp: Nach dem Erstellen öffnet sich der Editor und richtet das Template automatisch ein."
                 Foreground="{StaticResource App.Text.Muted}" DockPanel.Dock="Left" VerticalAlignment="Center"/>
            <StackPanel Margin="10,0,0,0" Orientation="Horizontal" DockPanel.Dock="Right">
                <Button Style="{StaticResource SecondaryButton}" Content="Beenden"
                Command="{Binding ExitCommand}" Margin="0,0,8,0"/>
                <Button Style="{StaticResource PrimaryButton}" Content="Erstellen"
                Command="{Binding CreateCommand}"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
